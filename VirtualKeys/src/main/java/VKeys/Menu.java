import java.util.Scanner;

public class Menu {

int index = 0;

    public menu() {
        //TODO: ask user for input on what they would like to do in the app
        //TODO: using a switch to change the case would work here (Check with previous assignemnt: Bug Fixes)
        
        Scanner input = new Scanner(System.in);
        int option = input.nextInt();
        switch (option) {
            case 1:
        }
    }
}